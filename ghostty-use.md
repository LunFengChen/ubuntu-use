# Ghostty 使用与美化

Ghostty 相关内容现在只在独立仓库维护，当前仓库通过 submodule 引入，避免两边重复修改：

```text
terminal/ghostty-use/
```

在线地址：

```text
https://github.com/LunFengChen/ghostty-use
```

本地查看：

```bash
git submodule update --init --recursive
cd terminal/ghostty-use
```
